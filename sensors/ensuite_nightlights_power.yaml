---
platform: template
sensors:
  ensuite_nightlights_power:
    device_class: power
    friendly_name: Ensuite Nightlights Power
    unit_of_measurement: W
    value_template: >-
      {{ (states('sensor.ensuite_nightlight_01_power') | float) }}
